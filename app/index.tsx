import { ChecklistMainList } from '@/features/checklist/components/ChecklistMainList';
import { StyleSheet } from 'react-native';


export default function HomeScreen() {
  return (
    <ChecklistMainList />
  );
}

const styles = StyleSheet.create({
});
